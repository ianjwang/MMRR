% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotMMRR.R
\name{plotMMRR}
\alias{plotMMRR}
\title{Plot MMMR}
\usage{
plotMMRR(reg, Y, X, scale = TRUE, varNames = NULL, lineCol = "blue", ...)
}
\arguments{
\item{reg}{The fitted MMRR model}

\item{Y}{The dependent variable in the form of a distance matrix}

\item{X}{A list of independent variables in the form of distance matrices (with optional names)}

\item{scale}{If TRUE, all variables are scaled}

\item{varNames}{A vector of names for the variables in the model (optional)}

\item{lineCol}{Color for regression line}

\item{...}{Additional arguments to be passed to plot() function (optional)}
}
\description{
Plots the results of an MMRR analysis
}
\details{
The objects supplied for Y and X should be the same variables used to fit the MMRR model.  The paramater 'scale' should be the same as used to fit the model.
The varNames argument can be used to specify variable names for labeling the plot axes.  The first name is for the dependent variable; additional names should be supplied in the same order as the independent variables.
}
\examples{
Xmats <- list(geography = simData$geoMat, ecology = simData$ecoMat)
reg <- MMRR(simData$genMat, Xmats, nperm = 99)
plotMMRR(reg, simData$genMat, Xmats, varNames = c("Gen. Dist.", "Geo. Dist.", "Env. Dist."))
}
